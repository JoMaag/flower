# Comparison: AFedPG / FedPG-BR — Byzantine filter + SCSG variance reduction
# CartPole-v1, 10 workers, no Byzantine
env = "CartPole-v1"
num-server-rounds = 312
num-workers = 10
num-byzantine = 0
method = "fedpg-br"
use-adaptive-communication = false
