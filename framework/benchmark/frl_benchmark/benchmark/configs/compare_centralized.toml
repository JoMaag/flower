# Comparison: Centralized baseline — single agent, K×batch (oracle upper bound)
# CartPole-v1, 1 worker, batch_size = 10 × 16 = 160
env = "CartPole-v1"
num-server-rounds = 312
num-workers = 1
num-byzantine = 0
method = "centralized"
batch-size = 160
use-adaptive-communication = false
