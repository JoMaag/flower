# Comparison: FedPG (GOMDP) — simple gradient averaging, no filtering
# Pursuit-v4, 10 workers, no Byzantine
env = "Pursuit-v4"
num-server-rounds = 200
num-workers = 10
num-byzantine = 0
method = "gomdp"
use-adaptive-communication = false
