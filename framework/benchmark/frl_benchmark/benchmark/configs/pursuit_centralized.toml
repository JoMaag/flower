# Comparison: Centralized baseline — single agent, K×batch (oracle upper bound)
# Pursuit-v4, 1 worker, batch_size = 10 × 16 = 160
env = "Pursuit-v4"
num-server-rounds = 200
num-workers = 1
num-byzantine = 0
method = "centralized"
batch-size = 160
batch-size-min = 160
batch-size-max = 160
use-adaptive-communication = false
